modules:

  - type: script
    snippets:
      - "mkdir -p /var/nix"
      - "ln -s /var/nix /nix"

  - type: dnf
    install:
      packages:
        - nix
        - nix-daemon

  - type: systemd
    system:
      enabled:
        - nix-daemon.service