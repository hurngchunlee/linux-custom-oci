modules:
  - type: files
    files:
      - source: usr/lib/tmpfiles.d/nix.conf
        destination: /usr/lib/tmpfiles.d/nix.conf

  - type: dnf
    install:
      packages:
        - nix
        - nix-daemon

  - type: systemd
    system:
      enabled:
        - nix-daemon.service